{
  "name": "chrome-extension",
  "version": "1.0.0",
  "description": "chrome extension test",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^5.0.2"
  }
}
