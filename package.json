{
  "name": "chrome-extension",
  "version": "1.0.0",
  "description": "chrome extension test",
  "private": true,
  "scripts": {
    "build": "npm run lint && npm run compile && npm run collect",
    "collect": "cp manifest.json dist/",
    "compile": "webpack --config webpack.config.js",
    "lint": "tslint scripts/**/*.ts"
  },
  "devDependencies": {
    "chrome-types": "^0.1.181",
    "html-webpack-plugin": "^5.5.0",
    "sass": "^1.60.0",
    "sass-loader": "^13.2.2",
    "ts-loader": "^9.4.2",
    "tslint": "^6.1.3",
    "typescript": "^5.0.2",
    "webpack": "^5.76.3",
    "webpack-cli": "^5.0.1"
  }
}
