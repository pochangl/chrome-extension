{
  "name": "chrome-extension",
  "version": "1.0.0",
  "description": "chrome extension test",
  "scripts": {
    "build": "npm run lint && npm run compile && npm run collect",
    "collect": "cp main.html dist/ && cp manifest.json dist/",
    "compile": "tsc -p tsconfig.json",
    "lint": "tslint scripts/**/*.ts"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^5.0.2",
    "webpack": "^5.76.3",
    "webpack-cli": "^5.0.1"
  },
  "dependencies": {
    "chrome-types": "^0.1.181"
  }
}
